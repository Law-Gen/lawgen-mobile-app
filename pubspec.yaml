name: lawgen
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  bloc_test: ^10.0.0
  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_client_sse: ^2.0.3
  flutter_secure_storage: ^9.2.4
  get_it: ^8.2.0
  go_router: ^16.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.5.0
  internet_connection_checker: ^3.0.1
  intl: ^0.20.2
  mockito: ^5.4.5
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  flutter_svg: ^2.2.0
  image_picker: ^1.0.7
  dio: ^5.9.0
  stylish_bottom_bar: ^1.0.3
  # Voice recording and playback

  just_audio: ^0.10.5
  path_provider: ^2.1.5
  google_sign_in: ^6.2.1 # Or any version starting with 6
  pkce: ^1.1.0
  # The following adds the Cupertino Icons font to your application.  shared_preferences: ^
  bloc: ^9.0.0
  url_launcher: ^6.1.14

dev_dependencies:
  build_runner: ^2.4.11
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo/logo.svg
    - assets/logo/google.jpg
    - assets/images/image.jpg
